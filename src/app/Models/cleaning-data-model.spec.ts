import { CleaningDataModel } from './cleaning-data-model';

describe('CleaningDataModel', () => {
  it('should create an instance', () => {
    expect(new CleaningDataModel()).toBeTruthy();
  });
});
