import { DDirective } from './d.directive';

describe('DDirective', () => {
  it('should create an instance', () => {
    const directive = new DDirective();
    expect(directive).toBeTruthy();
  });
});
